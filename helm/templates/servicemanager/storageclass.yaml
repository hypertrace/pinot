{{- if and .Values.servicemanager.enabled .Values.servicemanager.storageclass.enabled }}
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: {{ .Values.servicemanager.storageclass.name }}
  {{- with .Values.servicemanager.storageclass.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
provisioner: {{ .Values.servicemanager.storageclass.provisioner }}
reclaimPolicy: {{ .Values.servicemanager.storageclass.reclaimPolicy }}
volumeBindingMode: {{ .Values.servicemanager.storageclass.volumeBindingMode }}
{{- with .Values.servicemanager.storageclass.parameters }}
parameters:
  {{- toYaml . | nindent 2 }}
{{- end }}
{{- end }}
